#include <stdio.h>
#include <stdlib.h> /* a header file with a function we might need */

int main(int argc, char * argv[1]) /* Takes command line argument */
{
   printf("Hello %s\n",argv[1]);
   exit(0);
}
